# Backend environment variables (copy to .env or set in Render dashboard)
#
# Supabase — required for the data flywheel (quote collection + retraining).
# The API degrades gracefully (no quote saving) when these are absent.
SUPABASE_URL=https://apmkehezpxflljnocqsi.supabase.co
SUPABASE_SECRET_KEY=sb_secret_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: set to the deployed frontend origin to tighten CORS later.
# CORS_ALLOW_ORIGIN=https://your-frontend.vercel.app